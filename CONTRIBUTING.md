# راهنمای مشارکت در پروژه

## چگونه مشارکت کنید؟

ما از مشارکت شما در توسعه سیستم قرعه‌کشی آنلاین استقبال می‌کنیم!

## ۱. Fork کنید

از صفحه اصلی ریپازیتوری، دکمه Fork را بزنید.

## ۲. Clone کنید

```bash
git clone https://github.com/YOUR-USERNAME/Shansix.git
cd Shansix
```

## ۳. Branch جدید بسازید

```bash
git checkout -b feature/your-feature-name
```

## ۴. تغییرات را اعمال کنید

کدهای خود را تغییر دهید و مطمئن شوید که:
- کدها تمیز و مستند هستند
- از الگوی کامنت‌گذاری پروژه پیروی می‌کنید
- تست‌ها نوشته شده‌اند (اگر لازم است)
- تغییرات با کامیت‌های واضح ثبت شده‌اند

## ۵. کامیت کنید

```bash
git add .
git commit -m "feat: add your feature description"
```

## پیروی از الگوی کامیت‌گذاری:
- `feat:` ویژگی جدید
- `fix:` رفع باگ
- `docs:` تغییرات در مستندات
- `style:` تغییرات در استایل
- `refactor:` بازنویسی کد
- `test:` اضافه/تغییر تست
- `chore:` تغییرات ابزار و تنظیمات

## ۶. Push کنید

```bash
git push origin feature/your-feature-name
```

## ۷. Pull Request باز کنید

به صفحه اصلی ریپازیتوری بروید و دکمه New Pull Request را بزنید.

### عنوان PR:
- واضح و توصیفی باشد
- با الگوی کامیت‌گذاری شروع شود
- مثال: `feat: add user profile page`

### توضیحات PR:
- چه کاری انجام داده شده است؟
- چرا این تغییر لازم بود؟
- آیا باگ‌های مرتبط برطرف شده است؟
- اسکرین‌شات (اگر تغییر UI است)

## استانداردهای کد

### TypeScript
- استفاده از TypeScript strict mode
- تعریف proper types برای تمام متغیرها
- جلوگیری از any استفاده

### React/Next.js
- استفاده از functional components
- استفاده از hooks
- proper error boundaries

### Styling
- استفاده از Tailwind CSS
- پیروی از design tokens پروژه
- responsive design

### Security
- هرگز اطلاعات حساس را در کد commit نکنید
- استفاده از environment variables
- اعتبارسنجی تمام ورودی‌ها

## بررسی کد (Code Review)

ممکن است چند روز طول بکشد تا PR شما بررسی شود. لطفاً صبور باشید.

## سوالات؟

اگر سوالی دارید، یک Issue باز کنید و سوال خود را به وضوح بیان کنید.

---

با تشکر از مشارکت شما! 🙏
